{
  "name": "what-to-watch",
  "version": "1.0.0",
  "description": "What to watch",
  "scripts": {
    "install": "spago install",
    "build": "spago build",
    "serve": "parcel index.html",
    "test": "spago test",
    "dev:build": "spago build --watch & parcel index.html",
    "prod:build": "spago build && parcel build index.html",
    "upgrade": "spago upgrade-set"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "purescript": "^0.13.6"
  },
  "dependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  }
}
